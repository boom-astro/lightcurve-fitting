[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "lightcurve_fitting"
version = "0.1.0"
description = "Python bindings for the lightcurve-fitting Rust library"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
